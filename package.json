{
  "license": "private",
  "scripts": {
    "local": "gulp set --env=local",
    "dev": "gulp set --env=dev",
    "build-dev": "yarn local && "
  },
  "devDependencies": {
    "@types/gulp": "^4.0.10",
    "@types/node": "^18.11.12",
    "gulp": "^4.0.2",
    "prettier": "2.8.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
