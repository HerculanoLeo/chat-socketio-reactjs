import { Box, styled } from '@mui/material';

export const RoomCreateContainer = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'stretch',
  justifyContent: 'center',
  padding: '2rem 1rem',
  width: '100%'
}));
